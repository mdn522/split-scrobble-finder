{
    "buildCommand": "cd client && npm run build && cd ..",
    "installCommand": "cd client && npm install && cd .. && npm install",
    "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@now/node-server"
    }
  ],
"routes": [{
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "server.js"
    }
  ]
}